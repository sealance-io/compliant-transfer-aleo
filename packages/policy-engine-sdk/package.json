{
  "name": "@sealance-io/policy-engine-aleo",
  "version": "0.1.2",
  "description": "SDK for generating Merkle proofs and interacting with Aleo compliance policy programs",
  "author": "Sealance Team",
  "license": "Apache-2.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ],
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "tsc",
    "clean": "rimraf dist coverage",
    "prebuild": "npm run clean",
    "format": "prettier --check \"src/**/*.ts\" \"test/**/*.ts\"",
    "format:fix": "prettier --write \"src/**/*.ts\" \"test/**/*.ts\"",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "prepublishOnly": "npm run build",
    "build:examples": "npm run build && cd examples && npm install",
    "publish:github": "npm publish --registry=https://npm.pkg.github.com",
    "publish:npm": "npm publish --registry=https://registry.npmjs.org"
  },
  "keywords": [
    "aleo",
    "blockchain",
    "merkle-tree",
    "zero-knowledge",
    "compliance",
    "privacy"
  ],
  "dependencies": {
    "@provablehq/sdk": "^0.9.9",
    "@scure/base": "^2.0.0"
  },
  "devDependencies": {
    "@types/node": "24.9.1",
    "@vitest/coverage-v8": "^3.1.2",
    "prettier": "^3.7.4",
    "rimraf": "^6.0.1",
    "typescript": "^5.8.3",
    "vitest": "^3.1.2"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/sealance-io/compliant-transfer-aleo.git",
    "directory": "packages/policy-engine-sdk"
  }
}
