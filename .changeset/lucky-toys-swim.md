---
"@sealance-io/policy-engine-aleo": patch
---

Bump `@scure/base` to `^2.4.0`. Non-ASCII address strings that previously decoded via Unicode case folding are now rejected by `convertAddressToField`.
