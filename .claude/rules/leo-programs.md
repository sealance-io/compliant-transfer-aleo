---
paths:
  - "programs/**/*"
  - "artifacts/**/*"
---

# Leo Programs

See @docs/ARCHITECTURE.md for program structure.

**Critical constraints:**

- Compile with `dokojs compile` (not `leo build`)
- Leo CLI version: 3.4.0
- Never update `@doko-js/*` without checking `/patches`
